= simple_form_for(@commercial_offer) do |f|
  = f.error_notification
  .inputs
    = f.input :name
    = f.input :content
    -# - unless @contact
    = f.input :contact_id, :as => :select, :collection => Contact.all, :label_method => :name, :value_method => :id
    -# - else 
    -#   = f.input "contact", :as => :hidden
  .actions
    = f.button :submit
